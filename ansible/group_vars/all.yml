---
# Shared variables for all nodes
timezone: America/Los_Angeles    # Adjust to your timezone

# Inference endpoint (Orin Nano)
orin_ip: 10.1.10.221
llama_port: 8080
llama_endpoint: "http://{{ orin_ip }}:{{ llama_port }}/v1"

# Base packages installed on both nodes
common_packages:
  - curl
  - wget
  - git
  - htop
  - tmux
  - rsync
  - jq
  - python3
  - python3-pip
  - net-tools
  - iotop
  - unzip
