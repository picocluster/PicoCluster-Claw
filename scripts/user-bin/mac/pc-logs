#!/bin/bash
# pc-logs — Tail Docker container logs (Mac Solo)
set -euo pipefail

INSTALL_DIR="${HOME}/picocluster-claw"
cd "$INSTALL_DIR"
docker compose -f docker-compose.yml -f docker-compose.mac.yml logs -f --tail=100 "$@"
