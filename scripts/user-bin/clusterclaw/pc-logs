#!/bin/bash
# pc-logs — Tail PicoCluster Claw container logs
# Usage: pc-logs                # Tail all containers
#        pc-logs threadweaver   # Tail just one container
set -euo pipefail
cd /opt/clusterclaw
sudo docker compose logs -f --tail=100 "$@"
